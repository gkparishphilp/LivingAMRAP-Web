I"b(function() {
  jQuery.fn.inputfor = function(args) {
    if (args === void 0) {
      args = {};
    }
    return $(this).each(function() {
      var $for, $input;
      $input = $(this);
      if (window.console) {
        console.log('inputfor', $input);
      }
      $for = $($input.data('inputfor'));
      return $input.on('change keyup paste', function() {
        if (window.console) {
          console.log('inputfor', $input, 'change keyup paste');
        }
        return setTimeout(function() {
          return $for.each(function() {
            var $forItem;
            $forItem = $(this);
            if (window.console) {
              console.log('inputfor $forItem', $forItem);
            }
            if ($forItem.is('img')) {
              return $forItem.attr('src', $input.val());
            } else {
              return $forItem.val($input.val()).change();
            }
          });
        }, 5);
      });
    });
  };

  $(document).ready(function() {
    if (window.console) {
      console.log('inputfor ready');
    }
    return $('[data-inputfor]').inputfor();
  });

}).call(this);
:ET